Revision history for Perl extension Zengin-Pl

{{$NEXT}}

    - Rename the canonical module to Zengin::Pl
    - Keep Zengin::Client as a backward-compatible wrapper
    - Flatten repository layout so the distribution root matches the Git clone root
